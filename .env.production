# Variables de entorno para PRODUCCIÓN
# IMPORTANTE: No subir este archivo a GitHub

# Puerto del servidor (Railway/Render lo asigna automáticamente)
PORT=3001

# Entorno
NODE_ENV=production

# URL del frontend en producción (cambiar por tu dominio)
FRONTEND_URL=https://tu-app.vercel.app

# Base de datos (en producción será diferente)
DATABASE_URL=./costa_cat_kpis.db

# Configuración de archivos
MAX_FILE_SIZE=10485760
UPLOAD_DIR=uploads